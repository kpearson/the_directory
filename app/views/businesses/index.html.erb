<ul>
	<% @categories_b.each do |c| %>
		<%= c.name %>
		<% @businesses.each do |b| %>
			<ul><%= link_to(b.name, "/businesses/#{b.id}") %><br>
					<%= b.discription %></ul>
		<% end %>
		<br>
		<% if user_signed_in? %>
				<%= link_to "New", new_business_path %>
		<% end %>
	<% end %>
</ul>
<br>